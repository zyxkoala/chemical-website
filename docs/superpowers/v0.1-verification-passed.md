# V0.1 Verification — PASSED

Date: 2026-06-16

## Pipeline (`pnpm verify`)

```
pnpm lint                → No ESLint warnings or errors
pnpm test                → 32 tests passed across 5 files
pnpm check:placeholders  → 25 entries (4 placeholder products + 2 legal templates) — expected
pnpm check:no-fake-stats → OK: No fake stats found
pnpm check:no-forms      → OK: No <form> tags found
pnpm build               → 34 static routes exported successfully
```

Exit code: **0**

## Build Output

| Route | Type | Bundle |
|---|---|---|
| `/` | redirect → `/en` | 130 B |
| `/[locale]` (en, zh) | SSG | 1.55 kB |
| `/[locale]/products` | SSG | 12.2 kB (Fuse.js) |
| `/[locale]/products/[slug]` | SSG × 12 | 1.71 kB |
| `/[locale]/contact` | SSG | 2.22 kB |
| `/[locale]/privacy` | SSG | 165 B |
| `/[locale]/disclaimer` | SSG | 165 B |
| `/[locale]/{why-us,about,applications}` | SSG → 404 via `features.*=false` | 1.56 kB |
| `/sitemap.xml` | static | 22 URLs with hreflang |
| `/robots.txt` | static | sitemap reference |

First Load JS shared by all: **168 kB** (Sentry SDK adds ~65 kB to the 103 kB base — acceptable for V0.1).

## Go/No-Go Checklist (spec §13)

- [x] `pnpm verify` succeeds end-to-end
- [x] `/en` and `/zh` accessible at all 6 enabled routes
- [x] Disabled pages (`/why-us`, `/about`, `/applications`) render the localized not-found
- [x] `sitemap.xml` — 22 URLs (3 main × 2 + 2 legal × 2 + 6 products × 2), each with `hreflang en|zh` alternates; no disabled routes
- [x] `robots.txt` allows all and links to sitemap
- [x] Each page HTML head contains `rel="canonical"` + `hrefLang="en|zh|x-default"` + `og:locale`
- [x] Contact page contains no `<form>` tag (grep clean + script-blocked)
- [x] No fake stats anywhere (20+/15+/100% Customer/etc.)
- [x] `check:placeholders` output is exactly the expected 4 placeholder products + 2 legal templates
- [x] All 32 unit tests pass

## Outstanding

- Lighthouse pass (`/en`, `/en/products`) — **manual**, run against `pnpm exec serve out -l 3001` before deploy
- 360px responsive walk-through — **manual**
- Real-tracker smoke test — only meaningful with real `NEXT_PUBLIC_*` env vars in Vercel

## Ready for deployment

Next steps:
1. Push to `main`
2. Configure Vercel env vars: `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_CF_BEACON_TOKEN`, `NEXT_PUBLIC_SENTRY_DSN`
3. Vercel auto-deploys on push to main
4. Verify production Analytics + Sentry capture after first deploy
5. Submit `sitemap.xml` to Google Search Console
