# Phase 10 — Sentry Integration

## Task 33: Sentry wizard + config

**Files:**
- Create: `sentry.client.config.ts`
- Create: `sentry.server.config.ts`
- Modify: `next.config.ts` (add withSentryConfig wrapper)
- Modify: `src/app/[locale]/error.tsx` (already has `Sentry.captureException` — verify)

- [ ] **Step 1: Run Sentry wizard**

```bash
npx @sentry/wizard@latest -i nextjs --skip-connect
```

Follow prompts:
- Enable client + server (no edge — static export doesn't use edge)
- When asked for DSN: skip (we'll use env var)
- Let it modify `next.config.ts` and create config files

- [ ] **Step 2: Edit `sentry.client.config.ts`** — set sane defaults for V0.1

```ts
import * as Sentry from '@sentry/nextjs';

Sentry.init({
  dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
  enabled: process.env.NODE_ENV === 'production',
  tracesSampleRate: 0, // V0.1 关 performance monitoring 节省额度
  replaysSessionSampleRate: 0,
  replaysOnErrorSampleRate: 0,
});
```

- [ ] **Step 3: Edit `sentry.server.config.ts`** — same defaults

```ts
import * as Sentry from '@sentry/nextjs';

Sentry.init({
  dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
  enabled: process.env.NODE_ENV === 'production',
  tracesSampleRate: 0,
});
```

- [ ] **Step 4: Verify `src/app/[locale]/error.tsx` calls `Sentry.captureException`** — already added in Task 23 Phase 7. Confirm it's there.

- [ ] **Step 5: Test Sentry capture in dev**

Temporarily add a button to Home that throws an error:
```tsx
<button onClick={() => { throw new Error('Test Sentry'); }}>Break</button>
```
Click it → error.tsx renders → check console for "Test Sentry" logged (Sentry doesn't send in dev, but `captureException` runs).

Remove the test button.

- [ ] **Step 6: Commit**

```bash
git add sentry.client.config.ts sentry.server.config.ts next.config.ts
git commit -m "feat(sentry): configure client + server error tracking"
```

---
