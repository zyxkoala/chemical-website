# AOWATT V0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship V0.1 of AOWATT Global Materials chemical B2B website — Next.js App Router static site with bilingual `/en` `/zh` routes, 6 enabled pages (Home, Products, Product Detail, Contact, Privacy, Disclaimer), 3 disabled pages written behind feature flags, full SEO/Analytics/Sentry wiring, and verification scripts.

**Architecture:** Static-only Next.js (`output: 'export'`) deployed to Vercel. Pages read from typed local content files via a data-access layer. UI styled with Tailwind v4 against tokens from `CLAUDE.md`. i18n via next-intl with route-level `/[locale]/`. Visual placeholders use one shared inline-SVG component (`PlaceholderVisual`) — no raster images needed at V0.1.

**Tech Stack:** Next.js 15 (App Router) · TypeScript 5 · Tailwind CSS 4 · next-intl 3 · fuse.js 7 · @vercel/analytics · @sentry/nextjs · marked (legal pages) · Vitest (unit tests for `lib/`).

**Spec:** `docs/superpowers/specs/2026-06-15-aowatt-v0.1-design.md`

**TDD scope:** Unit tests cover `src/lib/*` (logic) and `scripts/*` (verification scripts). Components are validated via `pnpm build` + Lighthouse + manual walk-through per spec §12. This is honest about where automated tests add signal — DOM snapshots on a static marketing site rot fast and don't catch what matters (visual fidelity, SEO output, link correctness).

**Convention:** All commits use Conventional Commits (`feat:` / `chore:` / `test:` / `fix:` / `docs:`). Commit after every task.

---

## Phase 1 — Scaffold

### Task 1: Initialize Next.js project in repo root

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `next.config.ts`
- Create: `.gitignore` (extend existing)
- Create: `.env.local.example`

- [ ] **Step 1: Write `package.json`**

```json
{
  "name": "aowatt-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:placeholders": "tsx scripts/check-placeholders.ts",
    "check:no-fake-stats": "tsx scripts/check-no-fake-stats.ts",
    "check:no-forms": "tsx scripts/check-no-forms.ts",
    "verify": "pnpm lint && pnpm test && pnpm check:placeholders && pnpm check:no-fake-stats && pnpm check:no-forms && pnpm build"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next-intl": "^3.26.0",
    "fuse.js": "^7.0.0",
    "@vercel/analytics": "^1.4.0",
    "@sentry/nextjs": "^8.0.0",
    "marked": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^15.0.0",
    "prettier": "^3.3.0",
    "prettier-plugin-tailwindcss": "^0.6.0",
    "tsx": "^4.19.0",
    "glob": "^11.0.0",
    "vitest": "^2.1.0",
    "@vitest/ui": "^2.1.0"
  },
  "packageManager": "pnpm@9.12.0"
}
```

- [ ] **Step 2: Write `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules", "out", ".next"]
}
```

- [ ] **Step 3: Write `next.config.ts`**

```ts
import type { NextConfig } from 'next';
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

const nextConfig: NextConfig = {
  output: 'export',
  trailingSlash: true,
  images: { unoptimized: true },
  reactStrictMode: true,
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 4: Update `.gitignore`** (append the following — keep existing entries)

```
# Next.js
.next/
out/
next-env.d.ts

# Env
.env.local
.env*.local

# Vercel
.vercel/

# pnpm
node_modules/
```

- [ ] **Step 5: Write `.env.local.example`**

```
NEXT_PUBLIC_SITE_URL=https://aowatt.com.au
NEXT_PUBLIC_CF_BEACON_TOKEN=
NEXT_PUBLIC_SENTRY_DSN=
```

- [ ] **Step 6: Install deps**

Run: `pnpm install`
Expected: install completes, `node_modules/` populated, no peer-dep errors.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-lock.yaml tsconfig.json next.config.ts .gitignore .env.local.example
git commit -m "chore: scaffold Next.js project for AOWATT V0.1"
```

---

### Task 2: Tailwind config + globals + Inter font

**Files:**
- Create: `tailwind.config.ts`
- Create: `postcss.config.mjs`
- Create: `src/app/globals.css`

- [ ] **Step 1: Write `tailwind.config.ts`**

```ts
import type { Config } from 'tailwindcss';

export default {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        navy: '#010912',
        'navy-deep': '#091C1C',
        'navy-strip': '#000D0D',
        gold: '#F09F14',
        'blue-primary': '#053B7D',
        'gray-body': '#5C667A',
        'gray-light': '#D1E0F0',
        'border-light': '#DBE0EB',
      },
      fontFamily: { sans: ['var(--font-inter)', 'system-ui', 'sans-serif'] },
      fontSize: {
        hero: ['56px', { lineHeight: '68px', fontWeight: '700' }],
        'page-title': ['52px', { lineHeight: '62px', fontWeight: '700' }],
        section: ['34px', { lineHeight: '42px', fontWeight: '700' }],
        'card-title': ['19px', { lineHeight: '25px', fontWeight: '600' }],
        'why-title': ['17px', { lineHeight: '24px', fontWeight: '600' }],
        subtitle: ['19px', { lineHeight: '30px' }],
        body: ['15px', { lineHeight: '23px' }],
        nav: ['15px', { lineHeight: '20px', fontWeight: '600' }],
        'footer-link': ['15px', { lineHeight: '26px' }],
        legal: ['13px', { lineHeight: '18px' }],
      },
      maxWidth: { 'page-max': '1440px' },
      spacing: {
        'section-px': '80px',
        'header-h': '126px',
        'header-px': '78px',
      },
      borderRadius: { card: '7px', button: '4px' },
      height: { button: '48px' },
    },
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 2: Write `postcss.config.mjs`**

```js
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 3: Write `src/app/globals.css`**

```css
@import 'tailwindcss';

body {
  @apply text-navy-deep bg-white antialiased;
  font-feature-settings: 'cv11', 'ss01';
}

* {
  -webkit-font-smoothing: antialiased;
}
```

- [ ] **Step 4: Commit**

```bash
git add tailwind.config.ts postcss.config.mjs src/app/globals.css
git commit -m "chore: configure Tailwind v4 with AOWATT design tokens"
```

---

### Task 3: Vitest configuration

**Files:**
- Create: `vitest.config.ts`
- Create: `src/lib/__tests__/.gitkeep`

- [ ] **Step 1: Write `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/*.test.ts', 'scripts/**/*.test.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
});
```

- [ ] **Step 2: Verify Vitest runs**

Run: `pnpm test`
Expected: Vitest reports `No test files found, exiting with code 0` (we haven't written tests yet).

- [ ] **Step 3: Commit**

```bash
git add vitest.config.ts
git commit -m "chore: configure Vitest for lib/ unit tests"
```

---

## Phase 2 — Types, Content, Feature Flags

### Task 4: Type definitions

**Files:**
- Create: `src/types/locale.ts`
- Create: `src/types/product.ts`

- [ ] **Step 1: Write `src/types/locale.ts`**

```ts
export type Locale = 'en' | 'zh';

export const LOCALES: readonly Locale[] = ['en', 'zh'] as const;
export const DEFAULT_LOCALE: Locale = 'en';
```

- [ ] **Step 2: Write `src/types/product.ts`**

```ts
import type { Locale } from './locale';

export type LocalizedString = { en: string; zh?: string };

export type ProductDocumentType = 'COA' | 'TDS' | 'SDS';

export type ProductSpec = {
  label: LocalizedString;
  value: LocalizedString;
};

export type PlaceholderVariant =
  | 'hero-plant'
  | 'page-hero'
  | 'category-industrial'
  | 'category-specialty'
  | 'category-solvents'
  | 'category-water'
  | 'category-agricultural'
  | 'category-mining'
  | 'product-default'
  | 'application-mining'
  | 'application-agriculture'
  | 'application-manufacturing'
  | 'application-water'
  | 'application-coatings'
  | 'application-logistics'
  | 'about-overview';

export type Product = {
  slug: string;
  name: LocalizedString;
  category: string;
  casNo?: string;
  summary: LocalizedString;
  overview: LocalizedString;
  grade?: LocalizedString;
  purity?: string;
  packaging: LocalizedString[];
  applications: LocalizedString[];
  documents: ProductDocumentType[];
  specs: ProductSpec[];
  relatedProductSlugs: string[];
  image: PlaceholderVariant;
  heroImage?: PlaceholderVariant;
  featured: boolean;
  published: boolean;
  placeholder?: boolean;
  sortOrder: number;
  seoTitle?: LocalizedString;
  seoDescription?: LocalizedString;
};

export type ProductCategory = {
  slug: string;
  name: LocalizedString;
  summary: LocalizedString;
  image: PlaceholderVariant;
  sortOrder: number;
  enabled: boolean;
};

export type LocalizedProduct = {
  slug: string;
  name: string;
  category: string;
  casNo?: string;
  summary: string;
  overview: string;
  grade?: string;
  purity?: string;
  packaging: string[];
  applications: string[];
  documents: ProductDocumentType[];
  specs: { label: string; value: string }[];
  relatedProductSlugs: string[];
  image: PlaceholderVariant;
  heroImage?: PlaceholderVariant;
  featured: boolean;
  published: boolean;
  placeholder?: boolean;
  sortOrder: number;
  seoTitle?: string;
  seoDescription?: string;
};

export type LocalizedCategory = {
  slug: string;
  name: string;
  summary: string;
  image: PlaceholderVariant;
  sortOrder: number;
  enabled: boolean;
};

export type { Locale };
```

- [ ] **Step 3: Commit**

```bash
git add src/types/
git commit -m "feat(types): define Product, Category, Locale types"
```

---

### Task 5: Site info + categories content

**Files:**
- Create: `src/content/site.ts`
- Create: `src/content/categories.ts`

- [ ] **Step 1: Write `src/content/site.ts`**

```ts
export const site = {
  name: 'AOWATT Global Materials',
  domain: 'aowatt.com.au',
  tagline: {
    en: 'Connecting materials worldwide with quality, reliability and integrity',
    zh: '以品质、可靠和诚信连接全球材料供应',
  },
  email: 'info@aowatt.com.au',
  phone: '+61451875076',
  phoneDisplay: '+61 451 875 076',
  whatsappUrl: 'https://wa.me/61451875076',
  address: '11 Bale Cct, Southbank VIC 3006, Australia',
  mapUrl: 'https://maps.google.com/?q=11+Bale+Cct+Southbank+VIC+3006',
} as const;
```

- [ ] **Step 2: Write `src/content/categories.ts`**

```ts
import type { ProductCategory } from '@/types/product';

export const categories: ProductCategory[] = [
  {
    slug: 'industrial-chemicals',
    name: { en: 'Industrial Chemicals', zh: '工业化学品' },
    summary: {
      en: 'Bulk acids, alkalis, and core process chemicals for manufacturing.',
      zh: '面向制造业的散装酸碱与核心工艺化学品。',
    },
    image: 'category-industrial',
    sortOrder: 1,
    enabled: true,
  },
  {
    slug: 'specialty-chemicals',
    name: { en: 'Specialty Chemicals', zh: '特种化学品' },
    summary: {
      en: 'Performance additives and intermediates for specialised industries.',
      zh: '面向特殊行业的高性能添加剂与中间体。',
    },
    image: 'category-specialty',
    sortOrder: 2,
    enabled: true,
  },
  {
    slug: 'solvents',
    name: { en: 'Solvents', zh: '溶剂' },
    summary: {
      en: 'Industrial solvents for cleaning, formulation, and extraction.',
      zh: '面向清洗、配方与提取的工业溶剂。',
    },
    image: 'category-solvents',
    sortOrder: 3,
    enabled: true,
  },
  {
    slug: 'water-treatment',
    name: { en: 'Water Treatment', zh: '水处理' },
    summary: {
      en: 'Disinfectants, coagulants, and pH adjusters for water systems.',
      zh: '面向水系统的消毒剂、絮凝剂与 pH 调节剂。',
    },
    image: 'category-water',
    sortOrder: 4,
    enabled: true,
  },
  {
    slug: 'agricultural-chemicals',
    name: { en: 'Agricultural Chemicals', zh: '农业化学品' },
    summary: {
      en: 'Inputs for crop nutrition, soil treatment, and post-harvest handling.',
      zh: '面向作物营养、土壤处理与采后处理的农业投入品。',
    },
    image: 'category-agricultural',
    sortOrder: 5,
    enabled: true,
  },
  {
    slug: 'mining-chemicals',
    name: { en: 'Mining Chemicals', zh: '矿业化学品' },
    summary: {
      en: 'Reagents and process chemicals for mineral extraction and beneficiation.',
      zh: '面向矿物提取与选矿的试剂与工艺化学品。',
    },
    image: 'category-mining',
    sortOrder: 6,
    enabled: true,
  },
];
```

- [ ] **Step 3: Commit**

```bash
git add src/content/site.ts src/content/categories.ts
git commit -m "feat(content): add site info and 6 product categories"
```

---

### Task 6: Product content (2 full + 4 placeholder)

**Files:**
- Create: `src/content/products.ts`

- [ ] **Step 1: Write `src/content/products.ts`** — see code below. The two filled-in products use real, publicly-available chemical data. The four placeholder products keep the schema honest while marking unfilled fields with `[PLACEHOLDER]`.

```ts
import type { Product } from '@/types/product';

export const products: Product[] = [
  {
    slug: 'caustic-soda-flakes-99',
    name: { en: 'Caustic Soda Flakes 99%', zh: '片碱 99%' },
    category: 'industrial-chemicals',
    casNo: '1310-73-2',
    summary: {
      en: 'High-purity sodium hydroxide in flake form for industrial use.',
      zh: '工业级高纯度片状氢氧化钠。',
    },
    overview: {
      en: 'Caustic soda flakes (sodium hydroxide, NaOH) at 99% min purity. Widely used in pulp & paper, textile processing, soap manufacturing, and water treatment. Strongly hygroscopic — store in dry, sealed containers away from acids.',
      zh: '99% 最小纯度的片状氢氧化钠（NaOH）。广泛用于制浆造纸、纺织、肥皂制造与水处理。强吸湿性，应存放于干燥密封容器中并远离酸性物质。',
    },
    grade: { en: 'Industrial', zh: '工业级' },
    purity: '99% min',
    packaging: [
      { en: '25kg PP woven bag', zh: '25 公斤 PP 编织袋' },
      { en: '1MT bulk bag (jumbo)', zh: '1 吨吨袋（吨包）' },
    ],
    applications: [
      { en: 'Pulp & paper manufacturing', zh: '制浆造纸' },
      { en: 'Textile processing', zh: '纺织加工' },
      { en: 'Soap & detergent manufacturing', zh: '肥皂与洗涤剂制造' },
      { en: 'Water treatment (pH adjustment)', zh: '水处理（pH 调节）' },
    ],
    documents: ['COA', 'TDS', 'SDS'],
    specs: [
      { label: { en: 'Purity (NaOH)', zh: '纯度 (NaOH)' }, value: { en: '99% min', zh: '≥ 99%' } },
      { label: { en: 'Appearance', zh: '外观' }, value: { en: 'White flakes', zh: '白色片状' } },
      { label: { en: 'Bulk density', zh: '堆积密度' }, value: { en: '~ 0.9 g/cm³', zh: '约 0.9 g/cm³' } },
      { label: { en: 'Solubility (water, 20°C)', zh: '水溶性 (20°C)' }, value: { en: '1110 g/L', zh: '1110 g/L' } },
      { label: { en: 'Storage', zh: '储存' }, value: { en: 'Dry, sealed, away from acids', zh: '干燥密封，远离酸性物质' } },
      { label: { en: 'Shelf life', zh: '保质期' }, value: { en: '24 months in sealed packaging', zh: '密封包装下 24 个月' } },
    ],
    relatedProductSlugs: ['hydrochloric-acid-31', 'sodium-hypochlorite', 'citric-acid'],
    image: 'product-default',
    heroImage: 'page-hero',
    featured: true,
    published: true,
    sortOrder: 1,
    seoTitle: {
      en: 'Caustic Soda Flakes 99% (NaOH, CAS 1310-73-2) — AOWATT',
      zh: '片碱 99%（氢氧化钠，CAS 1310-73-2）— AOWATT',
    },
    seoDescription: {
      en: 'Industrial caustic soda flakes 99% min purity, 25kg PP bag or 1MT bulk packaging. Contact AOWATT Global Materials for export quotation.',
      zh: '工业级片碱，99% 最小纯度，25 公斤袋或 1 吨吨袋包装。如需出口报价，请联系 AOWATT Global Materials。',
    },
  },
  {
    slug: 'hydrochloric-acid-31',
    name: { en: 'Hydrochloric Acid 31%', zh: '盐酸 31%' },
    category: 'industrial-chemicals',
    casNo: '7647-01-0',
    summary: {
      en: 'Industrial-grade hydrochloric acid solution at 31% concentration.',
      zh: '31% 浓度的工业级盐酸溶液。',
    },
    overview: {
      en: 'Hydrochloric acid (HCl) at 31% ±1% concentration. Standard reagent for steel pickling, water treatment pH adjustment, and food-grade processing (where grade-appropriate). Highly corrosive — handle with chemical-resistant equipment.',
      zh: '31% ±1% 浓度的盐酸（HCl）。是钢铁酸洗、水处理 pH 调节与食品加工（在相应等级下）的标准试剂。强腐蚀性，须使用耐化学品设备操作。',
    },
    grade: { en: 'Industrial', zh: '工业级' },
    purity: '31% ±1%',
    packaging: [
      { en: '200kg HDPE drum', zh: '200 公斤 HDPE 桶' },
      { en: '1100kg IBC tank', zh: '1100 公斤 IBC 吨桶' },
    ],
    applications: [
      { en: 'Steel pickling & metal cleaning', zh: '钢铁酸洗与金属清洗' },
      { en: 'Water treatment (pH adjustment)', zh: '水处理（pH 调节）' },
      { en: 'Chemical synthesis', zh: '化学合成' },
      { en: 'Food processing (grade-dependent)', zh: '食品加工（依等级）' },
    ],
    documents: ['COA', 'TDS', 'SDS'],
    specs: [
      { label: { en: 'Concentration (HCl)', zh: '浓度 (HCl)' }, value: { en: '31% ±1%', zh: '31% ±1%' } },
      { label: { en: 'Appearance', zh: '外观' }, value: { en: 'Clear yellowish liquid', zh: '透明微黄液体' } },
      { label: { en: 'Density (20°C)', zh: '密度 (20°C)' }, value: { en: '~ 1.16 g/cm³', zh: '约 1.16 g/cm³' } },
      { label: { en: 'Free chlorine', zh: '游离氯' }, value: { en: '< 30 ppm', zh: '< 30 ppm' } },
      { label: { en: 'Iron (Fe)', zh: '铁 (Fe)' }, value: { en: '< 5 ppm', zh: '< 5 ppm' } },
      { label: { en: 'Storage', zh: '储存' }, value: { en: 'Cool, ventilated, acid-resistant container', zh: '阴凉通风，耐酸容器储存' } },
    ],
    relatedProductSlugs: ['caustic-soda-flakes-99', 'sodium-hypochlorite', 'citric-acid'],
    image: 'product-default',
    heroImage: 'page-hero',
    featured: true,
    published: true,
    sortOrder: 2,
    seoTitle: {
      en: 'Hydrochloric Acid 31% (HCl, CAS 7647-01-0) — AOWATT',
      zh: '盐酸 31%（HCl，CAS 7647-01-0）— AOWATT',
    },
    seoDescription: {
      en: 'Industrial hydrochloric acid 31% in 200kg drum or 1100kg IBC. Contact AOWATT Global Materials for export quotation.',
      zh: '工业级盐酸 31%，200 公斤桶或 1100 公斤 IBC 包装。如需出口报价，请联系 AOWATT Global Materials。',
    },
  },
  {
    slug: 'sodium-hypochlorite',
    name: { en: 'Sodium Hypochlorite', zh: '次氯酸钠' },
    category: 'water-treatment',
    casNo: '7681-52-9',
    summary: {
      en: '[PLACEHOLDER] Industrial bleach for disinfection and water treatment.',
      zh: '[PLACEHOLDER] 用于消毒与水处理的工业漂白剂。',
    },
    overview: { en: '[PLACEHOLDER] Overview pending product data confirmation.', zh: '[PLACEHOLDER] 概述待产品数据确认。' },
    packaging: [{ en: '[PLACEHOLDER] Packaging TBC', zh: '[PLACEHOLDER] 包装待定' }],
    applications: [{ en: '[PLACEHOLDER] Applications TBC', zh: '[PLACEHOLDER] 应用待定' }],
    documents: ['COA', 'SDS'],
    specs: [
      { label: { en: '[PLACEHOLDER] Spec label', zh: '[PLACEHOLDER] 规格标签' }, value: { en: '[PLACEHOLDER] value', zh: '[PLACEHOLDER] 值' } },
    ],
    relatedProductSlugs: ['caustic-soda-flakes-99', 'hydrochloric-acid-31', 'citric-acid'],
    image: 'product-default',
    featured: true,
    published: true,
    placeholder: true,
    sortOrder: 3,
  },
  {
    slug: 'industrial-solvent-blend',
    name: { en: 'Industrial Solvent Blend', zh: '工业溶剂混合物' },
    category: 'solvents',
    summary: { en: '[PLACEHOLDER] Solvent mixture for industrial cleaning and formulation.', zh: '[PLACEHOLDER] 工业清洗与配方用溶剂混合物。' },
    overview: { en: '[PLACEHOLDER] Overview pending.', zh: '[PLACEHOLDER] 概述待定。' },
    packaging: [{ en: '[PLACEHOLDER] Packaging TBC', zh: '[PLACEHOLDER] 包装待定' }],
    applications: [{ en: '[PLACEHOLDER] Applications TBC', zh: '[PLACEHOLDER] 应用待定' }],
    documents: ['TDS', 'SDS'],
    specs: [
      { label: { en: '[PLACEHOLDER] Spec label', zh: '[PLACEHOLDER] 规格标签' }, value: { en: '[PLACEHOLDER] value', zh: '[PLACEHOLDER] 值' } },
    ],
    relatedProductSlugs: ['caustic-soda-flakes-99', 'hydrochloric-acid-31', 'citric-acid'],
    image: 'product-default',
    featured: true,
    published: true,
    placeholder: true,
    sortOrder: 4,
  },
  {
    slug: 'citric-acid',
    name: { en: 'Citric Acid', zh: '柠檬酸' },
    category: 'specialty-chemicals',
    casNo: '77-92-9',
    summary: { en: '[PLACEHOLDER] Specialty acid for food, beverage, and industrial use.', zh: '[PLACEHOLDER] 用于食品、饮料与工业的特种酸。' },
    overview: { en: '[PLACEHOLDER] Overview pending.', zh: '[PLACEHOLDER] 概述待定。' },
    packaging: [{ en: '[PLACEHOLDER] Packaging TBC', zh: '[PLACEHOLDER] 包装待定' }],
    applications: [{ en: '[PLACEHOLDER] Applications TBC', zh: '[PLACEHOLDER] 应用待定' }],
    documents: ['COA', 'TDS'],
    specs: [
      { label: { en: '[PLACEHOLDER] Spec label', zh: '[PLACEHOLDER] 规格标签' }, value: { en: '[PLACEHOLDER] value', zh: '[PLACEHOLDER] 值' } },
    ],
    relatedProductSlugs: ['caustic-soda-flakes-99', 'hydrochloric-acid-31', 'sodium-hypochlorite'],
    image: 'product-default',
    featured: true,
    published: true,
    placeholder: true,
    sortOrder: 5,
  },
  {
    slug: 'mining-collector-reagent',
    name: { en: 'Mining Collector Reagent', zh: '矿用捕收剂' },
    category: 'mining-chemicals',
    summary: { en: '[PLACEHOLDER] Flotation collector for mineral processing.', zh: '[PLACEHOLDER] 矿物加工浮选捕收剂。' },
    overview: { en: '[PLACEHOLDER] Overview pending.', zh: '[PLACEHOLDER] 概述待定。' },
    packaging: [{ en: '[PLACEHOLDER] Packaging TBC', zh: '[PLACEHOLDER] 包装待定' }],
    applications: [{ en: '[PLACEHOLDER] Applications TBC', zh: '[PLACEHOLDER] 应用待定' }],
    documents: ['SDS'],
    specs: [
      { label: { en: '[PLACEHOLDER] Spec label', zh: '[PLACEHOLDER] 规格标签' }, value: { en: '[PLACEHOLDER] value', zh: '[PLACEHOLDER] 值' } },
    ],
    relatedProductSlugs: ['caustic-soda-flakes-99', 'hydrochloric-acid-31', 'sodium-hypochlorite'],
    image: 'product-default',
    featured: true,
    published: true,
    placeholder: true,
    sortOrder: 6,
  },
];
```

- [ ] **Step 2: Commit**

```bash
git add src/content/products.ts
git commit -m "feat(content): add 6 V0.1 products (2 filled + 4 placeholder)"
```

---

### Task 7: Feature flags

**Files:**
- Create: `src/config/features.ts`
- Create: `src/config/__tests__/features.test.ts`

- [ ] **Step 1: Write the failing test `src/config/__tests__/features.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { features, navItems, enabledMainNavItems, enabledFooterNavItems } from '../features';

describe('features', () => {
  it('enables exactly the V0.1 launch pages', () => {
    expect(features.home).toBe(true);
    expect(features.products).toBe(true);
    expect(features.productDetail).toBe(true);
    expect(features.contact).toBe(true);
    expect(features.privacy).toBe(true);
    expect(features.disclaimer).toBe(true);
    expect(features.whyUs).toBe(false);
    expect(features.about).toBe(false);
    expect(features.applications).toBe(false);
    expect(features.resources).toBe(false);
  });
});

describe('enabledMainNavItems', () => {
  it('returns Home, Products, Contact only at V0.1', () => {
    const keys = enabledMainNavItems().map(i => i.key);
    expect(keys).toEqual(['home', 'products', 'contact']);
  });
});

describe('enabledFooterNavItems', () => {
  it('does not include disabled pages', () => {
    const keys = enabledFooterNavItems().map(i => i.key);
    expect(keys).not.toContain('whyUs');
    expect(keys).not.toContain('about');
    expect(keys).not.toContain('applications');
  });

  it('includes the three enabled main-nav pages', () => {
    const keys = enabledFooterNavItems().map(i => i.key);
    expect(keys).toContain('home');
    expect(keys).toContain('products');
    expect(keys).toContain('contact');
  });
});

describe('navItems', () => {
  it('does not include privacy or disclaimer (footer-strip only)', () => {
    const keys = navItems.map(i => i.key);
    expect(keys).not.toContain('privacy');
    expect(keys).not.toContain('disclaimer');
  });
});
```

- [ ] **Step 2: Run the test, expect failure**

Run: `pnpm test`
Expected: FAIL — `Cannot find module '../features'`.

- [ ] **Step 3: Implement `src/config/features.ts`**

```ts
export type PageKey =
  | 'home' | 'products' | 'productDetail' | 'contact'
  | 'privacy' | 'disclaimer'
  | 'whyUs' | 'about' | 'applications' | 'resources';

export const features: Record<PageKey, boolean> = {
  home: true,
  products: true,
  productDetail: true,
  contact: true,
  privacy: true,
  disclaimer: true,
  whyUs: false,
  about: false,
  applications: false,
  resources: false,
};

export type NavItem = {
  key: PageKey;
  href: string;
  labelKey: string;
  inMainNav: boolean;
};

export const navItems: readonly NavItem[] = [
  { key: 'home', href: '/', labelKey: 'nav.home', inMainNav: true },
  { key: 'products', href: '/products', labelKey: 'nav.products', inMainNav: true },
  { key: 'whyUs', href: '/why-us', labelKey: 'nav.whyUs', inMainNav: false },
  { key: 'about', href: '/about', labelKey: 'nav.about', inMainNav: false },
  { key: 'applications', href: '/applications', labelKey: 'nav.applications', inMainNav: false },
  { key: 'contact', href: '/contact', labelKey: 'nav.contact', inMainNav: true },
] as const;

export function enabledMainNavItems(): NavItem[] {
  return navItems.filter(i => features[i.key] && i.inMainNav);
}

export function enabledFooterNavItems(): NavItem[] {
  return navItems.filter(i => features[i.key]);
}
```

- [ ] **Step 4: Run the test, expect pass**

Run: `pnpm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/config/features.ts src/config/__tests__/features.test.ts
git commit -m "feat(config): add page feature flags + main/footer nav helpers"
```

---

### Task 8: i18n messages

**Files:**
- Create: `src/messages/en.json`
- Create: `src/messages/zh.json`

- [ ] **Step 1: Write `src/messages/en.json`** — see full content below. Keys are organised by namespace, mirroring spec §5.7.

```json
{
  "nav": {
    "home": "Home",
    "products": "Products",
    "whyUs": "Why Us",
    "about": "About",
    "applications": "Applications",
    "contact": "Contact"
  },
  "common": {
    "zhFallbackBanner": "中文版正在更新，部分内容暂以英文显示。"
  },
  "button": {
    "explore": "Explore Products",
    "contactTeam": "Contact Our Team",
    "emailNow": "Email Now",
    "callNow": "Call Now",
    "openWhatsApp": "Open WhatsApp",
    "viewMap": "View on Map",
    "viewDetail": "View Detail",
    "emailOurTeam": "Email Our Team",
    "backHome": "Back to Home",
    "reload": "Reload"
  },
  "home": {
    "hero": {
      "title": "Reliable Chemical Supply for Global Industries",
      "subtitle": "AOWATT Global Materials connects buyers worldwide with verified chemical products, transparent documentation, and responsive export support."
    },
    "categories": {
      "heading": "Product Categories"
    },
    "whyUs": {
      "heading": "Why Work With Us",
      "qualityTitle": "Quality Documents",
      "qualityBody": "Every shipment is backed by COA, TDS and SDS — issued by the manufacturer and verifiable on request.",
      "packagingTitle": "Export Packaging",
      "packagingBody": "Pallet, drum, IBC and bulk-bag options sized for international logistics and port handling.",
      "supportTitle": "Responsive Support",
      "supportBody": "A direct contact team that handles enquiries, documentation, and post-shipment support without intermediaries."
    },
    "applications": {
      "heading": "Applications"
    }
  },
  "products": {
    "pageHero": {
      "eyebrow": "Catalog",
      "title": "Products",
      "subtitle": "Browse our chemical product range. Contact us for current pricing, full documentation, and packaging options."
    },
    "categoriesHeading": "Browse by Category",
    "featuredHeading": "Featured Products",
    "search": {
      "placeholder": "Search by name, CAS number, or category",
      "empty": "No products match. Try a different keyword or contact our team."
    },
    "inquiryCTA": {
      "title": "Need a product not listed?",
      "copy": "Tell us your specification and target volume — our team will reply with availability and pricing.",
      "button": "Contact Our Team"
    }
  },
  "productDetail": {
    "breadcrumbHome": "Home",
    "breadcrumbProducts": "Products",
    "specsHeading": "Specifications",
    "inquiryHeading": "Request a quotation",
    "inquiryCopy": "Send your target volume, packaging preference, and destination port. We will reply with current pricing and lead time.",
    "relatedHeading": "Related Products"
  },
  "contact": {
    "pageHero": {
      "eyebrow": "Get in touch",
      "title": "Contact Us",
      "subtitle": "Talk directly to our export team — we reply during Australian business hours."
    },
    "intro": "Reach us by email, phone, WhatsApp, or visit our Melbourne office.",
    "email": {
      "label": "Email",
      "help": "Best for product enquiries with specifications and volume."
    },
    "phone": {
      "label": "Phone",
      "help": "AEST business hours, Monday to Friday."
    },
    "whatsapp": {
      "label": "WhatsApp",
      "help": "Fastest channel for international buyers."
    },
    "address": {
      "label": "Address",
      "help": "Office visits by appointment."
    },
    "unlistedCTA": {
      "title": "Need a product not listed?",
      "copy": "Tell us what you are sourcing and we will check availability with our manufacturer network.",
      "button": "Email Our Team"
    },
    "guidanceHeading": "How we can help",
    "guidance": {
      "requirementsTitle": "Product requirements",
      "requirementsCopy": "Specification, purity, packaging, and destination — we shape a quotation around your needs.",
      "documentsTitle": "Document requests",
      "documentsCopy": "COA, TDS, and SDS available on request for any listed product.",
      "logisticsTitle": "Packaging and logistics",
      "logisticsCopy": "Drum, IBC, and bulk-bag options coordinated with your forwarder."
    }
  },
  "whyUs": {
    "pageHero": {
      "eyebrow": "Why work with us",
      "title": "Why Work With Us",
      "subtitle": "Three commitments that shape every shipment."
    },
    "advantagesHeading": "Core Advantages",
    "processHeading": "Cooperation Process",
    "process": {
      "step1": "Inquiry",
      "step2": "Requirement Review",
      "step3": "Document Confirmation",
      "step4": "Packaging & Logistics",
      "step5": "After-sales Support"
    },
    "supportCTA": {
      "title": "Need B2B sourcing support?",
      "copy": "Send us your specification and we will respond with documentation and lead time.",
      "button": "Contact Our Team"
    }
  },
  "about": {
    "pageHero": {
      "eyebrow": "About AOWATT",
      "title": "About AOWATT",
      "subtitle": "Connecting materials worldwide with quality, reliability and integrity."
    },
    "overviewHeading": "Company Overview",
    "overviewBody": "AOWATT Global Materials supplies industrial and specialty chemicals to buyers worldwide. We work directly with manufacturers, hold export documentation to international standards, and route shipments through verified logistics partners.",
    "categoriesBandHeading": "Categories We Serve",
    "valuesHeading": "Our Values",
    "values": {
      "qualityTitle": "Quality",
      "qualityCopy": "Documented specifications, manufacturer-issued COA on every shipment.",
      "reliabilityTitle": "Reliability",
      "reliabilityCopy": "Honest lead times, transparent packaging options, predictable communication.",
      "integrityTitle": "Integrity",
      "integrityCopy": "We will tell you when we are not the right fit. We will not overstate availability or capability."
    }
  },
  "applications": {
    "pageHero": {
      "eyebrow": "Industries",
      "title": "Industry Applications",
      "subtitle": "Six sectors we currently supply."
    },
    "items": {
      "miningTitle": "Mining",
      "miningCopy": "Reagents and process chemicals for mineral extraction and beneficiation.",
      "agricultureTitle": "Agriculture",
      "agricultureCopy": "Inputs for crop nutrition, soil treatment, and post-harvest handling.",
      "manufacturingTitle": "Manufacturing",
      "manufacturingCopy": "Bulk acids, alkalis, and process chemicals for industrial production.",
      "waterTitle": "Water Treatment",
      "waterCopy": "Disinfectants, coagulants, and pH adjusters for municipal and industrial water systems.",
      "coatingsTitle": "Coatings & Adhesives",
      "coatingsCopy": "Solvents and additives for paint, ink, and adhesive formulation.",
      "logisticsTitle": "Logistics & Packaging",
      "logisticsCopy": "Materials for industrial packaging, preservation, and dust suppression."
    },
    "cta": {
      "title": "Don't see your industry?",
      "copy": "Tell us your application — we likely supply something close.",
      "button": "Contact Our Team"
    }
  },
  "legal": {
    "privacyTitle": "Privacy Policy",
    "disclaimerTitle": "Disclaimer"
  },
  "footer": {
    "productsHeading": "Products",
    "contactHeading": "Contact",
    "copyright": "© {year} AOWATT Global Materials",
    "privacyLink": "Privacy",
    "disclaimerLink": "Disclaimer"
  },
  "errors": {
    "notFoundTitle": "Page not found",
    "notFoundCopy": "The page you are looking for has been moved or does not exist.",
    "errorTitle": "Something went wrong",
    "errorCopy": "An unexpected error occurred. Try reloading the page or contact us if it persists."
  }
}
```

- [ ] **Step 2: Write `src/messages/zh.json`** — same shape as en.json, all values translated. Disabled-page namespaces (`whyUs`, `about`, `applications`) may keep `[PLACEHOLDER]` Chinese values since pages return 404 in V0.1. Mirror every key from en.json — do not omit keys.

Use the following Chinese translations for the live namespaces. For `whyUs`, `about`, `applications`, copy values from en.json prefixed with `[PLACEHOLDER ZH] ` so they get caught by `check:placeholders`.

```json
{
  "nav": { "home": "首页", "products": "产品", "whyUs": "我们的优势", "about": "关于我们", "applications": "应用", "contact": "联系" },
  "common": { "zhFallbackBanner": "中文版正在更新，部分内容暂以英文显示。" },
  "button": {
    "explore": "浏览产品", "contactTeam": "联系我们", "emailNow": "立即邮件", "callNow": "立即致电",
    "openWhatsApp": "打开 WhatsApp", "viewMap": "查看地图", "viewDetail": "查看详情",
    "emailOurTeam": "邮件联系我们", "backHome": "返回首页", "reload": "重新加载"
  },
  "home": {
    "hero": {
      "title": "为全球工业提供可靠的化学品供应",
      "subtitle": "AOWATT Global Materials 为全球买家提供经过核验的化学品、透明的文档与响应式的出口支持。"
    },
    "categories": { "heading": "产品分类" },
    "whyUs": {
      "heading": "为何选择我们",
      "qualityTitle": "品质文档",
      "qualityBody": "每一批次均提供制造商签发的 COA、TDS 与 SDS，可按需核验。",
      "packagingTitle": "出口包装",
      "packagingBody": "提供托盘、桶装、IBC 与吨袋等多种规格，匹配国际物流与港口操作。",
      "supportTitle": "响应式支持",
      "supportBody": "由专属团队直接处理询价、文档与售后，无中间环节。"
    },
    "applications": { "heading": "应用领域" }
  },
  "products": {
    "pageHero": {
      "eyebrow": "产品目录",
      "title": "产品",
      "subtitle": "浏览我们的化学品产品系列。如需当前价格、完整文档与包装方案，请与我们联系。"
    },
    "categoriesHeading": "按分类浏览",
    "featuredHeading": "精选产品",
    "search": {
      "placeholder": "按名称、CAS 编号或分类搜索",
      "empty": "未找到匹配产品。请尝试其他关键词或联系我们的团队。"
    },
    "inquiryCTA": {
      "title": "未列出的产品？",
      "copy": "告诉我们您的规格与目标采购量，我们的团队将回复可用性与价格。",
      "button": "联系我们"
    }
  },
  "productDetail": {
    "breadcrumbHome": "首页",
    "breadcrumbProducts": "产品",
    "specsHeading": "规格",
    "inquiryHeading": "请求报价",
    "inquiryCopy": "请提供目标采购量、包装偏好与目的港，我们将回复当前价格与交期。",
    "relatedHeading": "相关产品"
  },
  "contact": {
    "pageHero": {
      "eyebrow": "联系我们",
      "title": "联系我们",
      "subtitle": "直接对话出口团队 — 我们在澳大利亚工作时间内回复。"
    },
    "intro": "可通过邮件、电话、WhatsApp 联系我们，或预约访问墨尔本办公室。",
    "email": { "label": "邮箱", "help": "适合附带规格与采购量的产品询盘。" },
    "phone": { "label": "电话", "help": "AEST 工作时间，周一至周五。" },
    "whatsapp": { "label": "WhatsApp", "help": "国际买家最快的沟通渠道。" },
    "address": { "label": "地址", "help": "如需到访请提前预约。" },
    "unlistedCTA": {
      "title": "未列出的产品？",
      "copy": "告诉我们您正在采购的产品，我们将与制造商网络确认可用性。",
      "button": "邮件联系我们"
    },
    "guidanceHeading": "我们如何提供帮助",
    "guidance": {
      "requirementsTitle": "产品需求",
      "requirementsCopy": "规格、纯度、包装、目的地 — 我们根据您的需求提供报价。",
      "documentsTitle": "文档索取",
      "documentsCopy": "任意产品均可按需提供 COA、TDS、SDS。",
      "logisticsTitle": "包装与物流",
      "logisticsCopy": "桶装、IBC、吨袋多种规格，与您的货代协同。"
    }
  },
  "whyUs": {
    "pageHero": { "eyebrow": "[PLACEHOLDER ZH] Why work with us", "title": "[PLACEHOLDER ZH] Why Work With Us", "subtitle": "[PLACEHOLDER ZH] Three commitments that shape every shipment." },
    "advantagesHeading": "[PLACEHOLDER ZH] Core Advantages",
    "processHeading": "[PLACEHOLDER ZH] Cooperation Process",
    "process": { "step1": "[PLACEHOLDER ZH] Inquiry", "step2": "[PLACEHOLDER ZH] Requirement Review", "step3": "[PLACEHOLDER ZH] Document Confirmation", "step4": "[PLACEHOLDER ZH] Packaging & Logistics", "step5": "[PLACEHOLDER ZH] After-sales Support" },
    "supportCTA": { "title": "[PLACEHOLDER ZH] Need B2B sourcing support?", "copy": "[PLACEHOLDER ZH] Send us your specification...", "button": "[PLACEHOLDER ZH] Contact Our Team" }
  },
  "about": {
    "pageHero": { "eyebrow": "[PLACEHOLDER ZH] About AOWATT", "title": "[PLACEHOLDER ZH] About AOWATT", "subtitle": "[PLACEHOLDER ZH] Connecting materials..." },
    "overviewHeading": "[PLACEHOLDER ZH] Company Overview",
    "overviewBody": "[PLACEHOLDER ZH] AOWATT Global Materials supplies...",
    "categoriesBandHeading": "[PLACEHOLDER ZH] Categories We Serve",
    "valuesHeading": "[PLACEHOLDER ZH] Our Values",
    "values": { "qualityTitle": "[PLACEHOLDER ZH] Quality", "qualityCopy": "[PLACEHOLDER ZH] ...", "reliabilityTitle": "[PLACEHOLDER ZH] Reliability", "reliabilityCopy": "[PLACEHOLDER ZH] ...", "integrityTitle": "[PLACEHOLDER ZH] Integrity", "integrityCopy": "[PLACEHOLDER ZH] ..." }
  },
  "applications": {
    "pageHero": { "eyebrow": "[PLACEHOLDER ZH] Industries", "title": "[PLACEHOLDER ZH] Industry Applications", "subtitle": "[PLACEHOLDER ZH] Six sectors we currently supply." },
    "items": {
      "miningTitle": "[PLACEHOLDER ZH] Mining", "miningCopy": "[PLACEHOLDER ZH] ...",
      "agricultureTitle": "[PLACEHOLDER ZH] Agriculture", "agricultureCopy": "[PLACEHOLDER ZH] ...",
      "manufacturingTitle": "[PLACEHOLDER ZH] Manufacturing", "manufacturingCopy": "[PLACEHOLDER ZH] ...",
      "waterTitle": "[PLACEHOLDER ZH] Water Treatment", "waterCopy": "[PLACEHOLDER ZH] ...",
      "coatingsTitle": "[PLACEHOLDER ZH] Coatings & Adhesives", "coatingsCopy": "[PLACEHOLDER ZH] ...",
      "logisticsTitle": "[PLACEHOLDER ZH] Logistics & Packaging", "logisticsCopy": "[PLACEHOLDER ZH] ..."
    },
    "cta": { "title": "[PLACEHOLDER ZH] Don't see your industry?", "copy": "[PLACEHOLDER ZH] ...", "button": "[PLACEHOLDER ZH] Contact Our Team" }
  },
  "legal": { "privacyTitle": "隐私政策", "disclaimerTitle": "免责声明" },
  "footer": {
    "productsHeading": "产品",
    "contactHeading": "联系",
    "copyright": "© {year} AOWATT Global Materials",
    "privacyLink": "隐私政策",
    "disclaimerLink": "免责声明"
  },
  "errors": {
    "notFoundTitle": "页面不存在",
    "notFoundCopy": "您访问的页面已被移动或不存在。",
    "errorTitle": "出现错误",
    "errorCopy": "发生意外错误。请重新加载页面，或与我们联系。"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/messages/
git commit -m "feat(i18n): add en/zh message namespaces"
```

---

## Phase 3 — Data Access Layer (TDD)

### Task 9: lib/i18n with tests

**Files:**
- Create: `src/lib/i18n.ts`
- Create: `src/lib/__tests__/i18n.test.ts`

- [ ] **Step 1: Write the failing test `src/lib/__tests__/i18n.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { pickLocalized, hasZhFallback, localizeProduct, localizeCategory } from '../i18n';
import type { Product, ProductCategory } from '@/types/product';

describe('pickLocalized', () => {
  it('returns en when locale is en', () => {
    expect(pickLocalized({ en: 'Hello', zh: '你好' }, 'en')).toBe('Hello');
  });
  it('returns zh when locale is zh and zh exists', () => {
    expect(pickLocalized({ en: 'Hello', zh: '你好' }, 'zh')).toBe('你好');
  });
  it('falls back to en when locale is zh and zh missing', () => {
    expect(pickLocalized({ en: 'Hello' }, 'zh')).toBe('Hello');
  });
});

describe('hasZhFallback', () => {
  it('returns false for en locale', () => {
    expect(hasZhFallback({ en: 'X' }, 'en')).toBe(false);
  });
  it('returns true when zh missing on zh locale', () => {
    expect(hasZhFallback({ en: 'X' }, 'zh')).toBe(true);
  });
  it('returns false when zh present on zh locale', () => {
    expect(hasZhFallback({ en: 'X', zh: 'Y' }, 'zh')).toBe(false);
  });
});

const sampleProduct: Product = {
  slug: 'sample',
  name: { en: 'Sample', zh: '样品' },
  category: 'industrial-chemicals',
  summary: { en: 'Sum' },
  overview: { en: 'Ovr' },
  packaging: [{ en: 'Bag', zh: '袋' }],
  applications: [{ en: 'Use' }],
  documents: [],
  specs: [{ label: { en: 'L' }, value: { en: 'V' } }],
  relatedProductSlugs: [],
  image: 'product-default',
  featured: false,
  published: true,
  sortOrder: 99,
};

describe('localizeProduct', () => {
  it('unwraps localized fields with fallback', () => {
    const p = localizeProduct(sampleProduct, 'zh');
    expect(p.name).toBe('样品');
    expect(p.summary).toBe('Sum');
    expect(p.packaging).toEqual(['袋']);
    expect(p.applications).toEqual(['Use']);
    expect(p.specs).toEqual([{ label: 'L', value: 'V' }]);
  });
});

describe('localizeCategory', () => {
  it('unwraps localized fields', () => {
    const cat: ProductCategory = {
      slug: 'x',
      name: { en: 'X', zh: 'X中' },
      summary: { en: 'sum', zh: '摘' },
      image: 'category-industrial',
      sortOrder: 1,
      enabled: true,
    };
    const out = localizeCategory(cat, 'zh');
    expect(out.name).toBe('X中');
    expect(out.summary).toBe('摘');
  });
});
```

- [ ] **Step 2: Run test, expect FAIL** (`Cannot find module '../i18n'`).

Run: `pnpm test`

- [ ] **Step 3: Implement `src/lib/i18n.ts`**

```ts
import type {
  Locale, LocalizedString, Product, ProductCategory,
  LocalizedProduct, LocalizedCategory,
} from '@/types/product';

export function pickLocalized<T>(s: { en: T; zh?: T }, locale: Locale): T {
  if (locale === 'zh' && s.zh !== undefined) return s.zh;
  return s.en;
}

export function hasZhFallback(s: { en: unknown; zh?: unknown }, locale: Locale): boolean {
  return locale === 'zh' && s.zh === undefined;
}

function pickArr(arr: LocalizedString[], locale: Locale): string[] {
  return arr.map(s => pickLocalized(s, locale));
}

export function localizeProduct(p: Product, locale: Locale): LocalizedProduct {
  return {
    slug: p.slug,
    name: pickLocalized(p.name, locale),
    category: p.category,
    casNo: p.casNo,
    summary: pickLocalized(p.summary, locale),
    overview: pickLocalized(p.overview, locale),
    grade: p.grade ? pickLocalized(p.grade, locale) : undefined,
    purity: p.purity,
    packaging: pickArr(p.packaging, locale),
    applications: pickArr(p.applications, locale),
    documents: p.documents,
    specs: p.specs.map(s => ({
      label: pickLocalized(s.label, locale),
      value: pickLocalized(s.value, locale),
    })),
    relatedProductSlugs: p.relatedProductSlugs,
    image: p.image,
    heroImage: p.heroImage,
    featured: p.featured,
    published: p.published,
    placeholder: p.placeholder,
    sortOrder: p.sortOrder,
    seoTitle: p.seoTitle ? pickLocalized(p.seoTitle, locale) : undefined,
    seoDescription: p.seoDescription ? pickLocalized(p.seoDescription, locale) : undefined,
  };
}

export function localizeCategory(c: ProductCategory, locale: Locale): LocalizedCategory {
  return {
    slug: c.slug,
    name: pickLocalized(c.name, locale),
    summary: pickLocalized(c.summary, locale),
    image: c.image,
    sortOrder: c.sortOrder,
    enabled: c.enabled,
  };
}
```

- [ ] **Step 4: Run test, expect PASS**

Run: `pnpm test`

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n.ts src/lib/__tests__/i18n.test.ts
git commit -m "feat(lib): add localize helpers + tests"
```

---

### Task 10: lib/products and lib/categories with tests

**Files:**
- Create: `src/lib/products.ts`
- Create: `src/lib/categories.ts`
- Create: `src/lib/__tests__/products.test.ts`
- Create: `src/lib/__tests__/categories.test.ts`

- [ ] **Step 1: Write the failing test `src/lib/__tests__/categories.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { getEnabledCategories, getCategoryBySlug } from '../categories';

describe('getEnabledCategories', () => {
  it('returns 6 categories sorted by sortOrder', () => {
    const cats = getEnabledCategories('en');
    expect(cats).toHaveLength(6);
    expect(cats.map(c => c.slug)).toEqual([
      'industrial-chemicals', 'specialty-chemicals', 'solvents',
      'water-treatment', 'agricultural-chemicals', 'mining-chemicals',
    ]);
  });

  it('localizes name when locale is zh', () => {
    const cats = getEnabledCategories('zh');
    expect(cats[0].name).toBe('工业化学品');
  });
});

describe('getCategoryBySlug', () => {
  it('returns the category by slug', () => {
    expect(getCategoryBySlug('water-treatment', 'en')?.slug).toBe('water-treatment');
  });
  it('returns null for unknown slug', () => {
    expect(getCategoryBySlug('nope', 'en')).toBeNull();
  });
});
```

- [ ] **Step 2: Implement `src/lib/categories.ts`**

```ts
import { categories } from '@/content/categories';
import { localizeCategory } from './i18n';
import type { Locale, LocalizedCategory } from '@/types/product';

export function getEnabledCategories(locale: Locale): LocalizedCategory[] {
  return categories
    .filter(c => c.enabled)
    .sort((a, b) => a.sortOrder - b.sortOrder)
    .map(c => localizeCategory(c, locale));
}

export function getCategoryBySlug(slug: string, locale: Locale): LocalizedCategory | null {
  const c = categories.find(c => c.slug === slug && c.enabled);
  return c ? localizeCategory(c, locale) : null;
}
```

- [ ] **Step 3: Run categories tests, expect PASS**

Run: `pnpm test src/lib/__tests__/categories.test.ts`

- [ ] **Step 4: Write the failing test `src/lib/__tests__/products.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import {
  getPublishedProducts, getFeaturedProducts, getProductBySlug,
  getProductsByCategory, getRelatedProducts, getProductStaticParams,
  searchProducts,
} from '../products';

describe('getPublishedProducts', () => {
  it('returns all 6 V0.1 products sorted', () => {
    const ps = getPublishedProducts('en');
    expect(ps).toHaveLength(6);
    expect(ps[0].slug).toBe('caustic-soda-flakes-99');
  });
  it('localizes the name when locale is zh', () => {
    const ps = getPublishedProducts('zh');
    expect(ps[0].name).toBe('片碱 99%');
  });
});

describe('getFeaturedProducts', () => {
  it('respects the limit parameter', () => {
    expect(getFeaturedProducts('en', 3)).toHaveLength(3);
  });
});

describe('getProductBySlug', () => {
  it('returns the product when it exists', () => {
    expect(getProductBySlug('caustic-soda-flakes-99', 'en')?.slug).toBe('caustic-soda-flakes-99');
  });
  it('returns null when slug unknown', () => {
    expect(getProductBySlug('does-not-exist', 'en')).toBeNull();
  });
});

describe('getProductsByCategory', () => {
  it('filters by category slug', () => {
    const ps = getProductsByCategory('industrial-chemicals', 'en');
    expect(ps.every(p => p.category === 'industrial-chemicals')).toBe(true);
    expect(ps.length).toBeGreaterThanOrEqual(2);
  });
});

describe('getRelatedProducts', () => {
  it('returns up to limit related products', () => {
    const p = getProductBySlug('caustic-soda-flakes-99', 'en')!;
    const related = getRelatedProducts(p, 'en', 3);
    expect(related.length).toBeLessThanOrEqual(3);
    expect(related.every(r => r.slug !== p.slug)).toBe(true);
  });
});

describe('getProductStaticParams', () => {
  it('returns locale × slug for all published products', () => {
    const params = getProductStaticParams();
    expect(params).toHaveLength(12); // 6 products × 2 locales
    expect(params).toContainEqual({ locale: 'en', slug: 'caustic-soda-flakes-99' });
    expect(params).toContainEqual({ locale: 'zh', slug: 'caustic-soda-flakes-99' });
  });
});

describe('searchProducts', () => {
  it('matches by name', () => {
    const r = searchProducts('caustic', 'en');
    expect(r.some(p => p.slug === 'caustic-soda-flakes-99')).toBe(true);
  });
  it('matches by CAS number', () => {
    const r = searchProducts('1310-73-2', 'en');
    expect(r.some(p => p.slug === 'caustic-soda-flakes-99')).toBe(true);
  });
  it('returns empty for no matches', () => {
    expect(searchProducts('xyz-no-such-thing', 'en')).toEqual([]);
  });
  it('returns full list for empty query', () => {
    expect(searchProducts('', 'en')).toHaveLength(6);
  });
});
```

- [ ] **Step 5: Implement `src/lib/products.ts`**

```ts
import Fuse from 'fuse.js';
import { products } from '@/content/products';
import { localizeProduct } from './i18n';
import { LOCALES } from '@/types/locale';
import type { Locale, LocalizedProduct } from '@/types/product';

function published(): typeof products {
  return products.filter(p => p.published).sort((a, b) => a.sortOrder - b.sortOrder);
}

export function getPublishedProducts(locale: Locale): LocalizedProduct[] {
  return published().map(p => localizeProduct(p, locale));
}

export function getFeaturedProducts(locale: Locale, limit = 6): LocalizedProduct[] {
  return published()
    .filter(p => p.featured)
    .slice(0, limit)
    .map(p => localizeProduct(p, locale));
}

export function getProductBySlug(slug: string, locale: Locale): LocalizedProduct | null {
  const p = published().find(p => p.slug === slug);
  return p ? localizeProduct(p, locale) : null;
}

export function getProductsByCategory(categorySlug: string, locale: Locale): LocalizedProduct[] {
  return published()
    .filter(p => p.category === categorySlug)
    .map(p => localizeProduct(p, locale));
}

export function getRelatedProducts(
  product: LocalizedProduct,
  locale: Locale,
  limit = 3,
): LocalizedProduct[] {
  const all = published();
  const explicit = product.relatedProductSlugs
    .map(slug => all.find(p => p.slug === slug))
    .filter((p): p is NonNullable<typeof p> => p != null && p.slug !== product.slug);

  const sameCategory = all
    .filter(p => p.category === product.category && p.slug !== product.slug
      && !explicit.some(e => e.slug === p.slug));

  return [...explicit, ...sameCategory]
    .slice(0, limit)
    .map(p => localizeProduct(p, locale));
}

export function getProductStaticParams(): { locale: Locale; slug: string }[] {
  return published().flatMap(p => LOCALES.map(locale => ({ locale, slug: p.slug })));
}

export function searchProducts(query: string, locale: Locale): LocalizedProduct[] {
  const all = getPublishedProducts(locale);
  const q = query.trim();
  if (q.length === 0) return all;

  const fuse = new Fuse(all, {
    keys: ['name', 'casNo', 'category', 'summary'],
    threshold: 0.35,
    ignoreLocation: true,
  });
  return fuse.search(q).map(r => r.item);
}
```

- [ ] **Step 6: Run all tests, expect PASS**

Run: `pnpm test`

- [ ] **Step 7: Commit**

```bash
git add src/lib/products.ts src/lib/categories.ts src/lib/__tests__/
git commit -m "feat(lib): add product/category data access + Fuse.js search"
```

---

### Task 11: lib/seo and lib/analytics

**Files:**
- Create: `src/lib/seo.ts`
- Create: `src/lib/analytics.ts`
- Create: `src/lib/__tests__/seo.test.ts`

- [ ] **Step 1: Write the failing test `src/lib/__tests__/seo.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { buildMetadata } from '../seo';

describe('buildMetadata', () => {
  it('builds canonical and hreflang alternates for en', () => {
    process.env.NEXT_PUBLIC_SITE_URL = 'https://aowatt.com.au';
    const m = buildMetadata({ title: 'X', description: 'Y', path: '/products', locale: 'en' });
    expect(m.alternates?.canonical).toBe('https://aowatt.com.au/en/products');
    expect((m.alternates?.languages as Record<string, string>)?.en).toBe('https://aowatt.com.au/en/products');
    expect((m.alternates?.languages as Record<string, string>)?.zh).toBe('https://aowatt.com.au/zh/products');
    expect((m.alternates?.languages as Record<string, string>)?.['x-default']).toBe('https://aowatt.com.au/en/products');
  });

  it('uses zh_CN OG locale for zh', () => {
    const m = buildMetadata({ title: 'X', description: 'Y', path: '/', locale: 'zh' });
    expect(m.openGraph?.locale).toBe('zh_CN');
  });

  it('uses en_AU OG locale for en', () => {
    const m = buildMetadata({ title: 'X', description: 'Y', path: '/', locale: 'en' });
    expect(m.openGraph?.locale).toBe('en_AU');
  });
});
```

- [ ] **Step 2: Implement `src/lib/seo.ts`**

```ts
import type { Metadata } from 'next';
import type { Locale } from '@/types/product';

type BuildArgs = {
  title: string;
  description: string;
  path: string; // begins with /
  locale: Locale;
  ogImage?: string;
};

export function buildMetadata({ title, description, path, locale, ogImage }: BuildArgs): Metadata {
  const base = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://aowatt.com.au';
  const url = `${base}/${locale}${path === '/' ? '' : path}`;
  return {
    title,
    description,
    alternates: {
      canonical: url,
      languages: {
        en: `${base}/en${path === '/' ? '' : path}`,
        zh: `${base}/zh${path === '/' ? '' : path}`,
        'x-default': `${base}/en${path === '/' ? '' : path}`,
      },
    },
    openGraph: {
      title,
      description,
      url,
      siteName: 'AOWATT Global Materials',
      locale: locale === 'zh' ? 'zh_CN' : 'en_AU',
      type: 'website',
      images: ogImage ? [{ url: ogImage }] : undefined,
    },
  };
}
```

- [ ] **Step 3: Run test, expect PASS**

Run: `pnpm test src/lib/__tests__/seo.test.ts`

- [ ] **Step 4: Implement `src/lib/analytics.ts`**

```ts
'use client';
import { track as vercelTrack } from '@vercel/analytics';

export type AnalyticsEvent =
  | 'product_card_click'
  | 'contact_email_click'
  | 'contact_phone_click'
  | 'contact_whatsapp_click'
  | 'search_query'
  | 'language_switch';

export function track(name: AnalyticsEvent, props: Record<string, string | number>): void {
  if (process.env.NODE_ENV === 'development') {
    console.debug('[track]', name, props);
    return;
  }
  vercelTrack(name, props);
}
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/seo.ts src/lib/analytics.ts src/lib/__tests__/seo.test.ts
git commit -m "feat(lib): add seo metadata builder + analytics track wrapper"
```

---

## Phase 4 — i18n routing

### Task 12: next-intl routing config

**Files:**
- Create: `src/i18n/routing.ts`
- Create: `src/i18n/request.ts`

- [ ] **Step 1: Write `src/i18n/routing.ts`**

```ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'zh'],
  defaultLocale: 'en',
  localePrefix: 'always',
});
```

- [ ] **Step 2: Write `src/i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = (routing.locales as readonly string[]).includes(requested ?? '')
    ? (requested as 'en' | 'zh')
    : routing.defaultLocale;

  const messages = (await import(`../messages/${locale}.json`)).default;
  return { locale, messages };
});
```

- [ ] **Step 3: Commit**

```bash
git add src/i18n/
git commit -m "feat(i18n): configure next-intl routing for en/zh"
```

---

## Plan Structure

This plan is split across multiple files for manageability:

- **This file (index.md)**: Overview + Phase 1-4 (Scaffold, Types/Content, Data Access Layer TDD, i18n routing)
- **[phase-5-ui-components.md](./2026-06-15-aowatt-v0.1-phase-5-ui-components.md)**: UI atoms (PlaceholderVisual, Button, SectionHeading, PageHero, TrackedLink)
- **[phase-6-layout.md](./2026-06-15-aowatt-v0.1-phase-6-layout.md)**: Layout components (Header, Footer, LanguageSwitcher, Analytics wrappers)
- **[phase-7-app-shell.md](./2026-06-15-aowatt-v0.1-phase-7-app-shell.md)**: Root + [locale] layouts, redirect, Inter font
- **[phase-8-pages.md](./2026-06-15-aowatt-v0.1-phase-8-pages.md)**: All 8 pages (Home, Products, Product Detail, Contact, Privacy, Disclaimer, Why Us, About, Applications)
- **[phase-9-seo.md](./2026-06-15-aowatt-v0.1-phase-9-seo.md)**: sitemap, robots, not-found, error pages, all generateMetadata
- **[phase-10-sentry.md](./2026-06-15-aowatt-v0.1-phase-10-sentry.md)**: Sentry client/server config
- **[phase-11-verification.md](./2026-06-15-aowatt-v0.1-phase-11-verification.md)**: Scripts (check-placeholders, check-no-fake-stats, check-no-forms) + public assets + final Go/No-Go

**Execution**: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans`. Load phases progressively as you complete earlier ones.

**Phase 1-4 完成后继续:** 执行完 Task 12 后，读取 `phase-5-ui-components.md` 开始 Phase 5。

---

